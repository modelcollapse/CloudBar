// helpers.js

function randomColor() {
  // This function is available if needed, but generateHarmonizedRandomColors 
  // in sketch.js is primarily used for the main shapes when useRandomColor is true.
  return color(random(360), 80, 100); // HSB color
}
// End of helpers.js